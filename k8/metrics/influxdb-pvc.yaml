kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: influxdb-storage-pvc
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
     storage: 1Gi

---

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: influxdb-entry-pvc
  labels:
    type: local
spec:
  storageClassName: standard
  volumeName: influxdb-entry-pv
  volumeMode: Filesystem
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
     storage: 1Gi
