kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: elasticsearch-storage-pvc
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
     storage: 1Gi
