kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: kibana-entry-pvc
  labels:
    type: local
spec:
  storageClassName: standard
  volumeName: kibana-entry-pv
  volumeMode: Filesystem
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
     storage: 1Gi
